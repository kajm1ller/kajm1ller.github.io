kajs website

## Running locally

Install dependencies with `npm install`, then start the live server with `npm start`. This runs `server.js`, which serves the site, API routes, and WebSocket endpoints on `http://localhost:3000` outside production.
